# 选择题   
1.b  
2.a  
3.a  
4.d  
5.c  
6.d  
7.c  
8.b  
9.b  
10.b   
11.d   
12.b  
13.d  
14.b  
15.c  
16.b   
17.a   
18.c   
19.b  
20.d   
# 编程  
1.   
```c  
#include<stdio.h>
#define   LED_PIN_0  0
void     led_on(uint8_t led_id);    // 点亮指定LED
void     led_off(uint8_t led_id);
void     delay_ms(uint16_t ms);      
int main()
{
    int i=0;
    while(1)
    {
        for(i=0;i<=7;i++)
        {
            led_on(led_i);
            delay_ms(300);
            led_dff(led_i);
            if(i>7)
            {
                i=0;
            }
        }
    }
    return 0;
}
```


2.   
```c
#include<stdio.h.>
#define   KEY_PIN_0  0
uint8_t  key_read_raw(uint8_t key_id); 
void     delay_ms(uint16_t ms);      
int main()
{
    uint8_t  key_read_raw(key0); 
    if(key0==0)
    {
        delay_ms(20);
        while(key0==0)
        delay_ms(20);
    led0=~led0;
    }
    return 0;
}
```  
3.   
```c   
#include<stdio.h>
void     gpio_port_write(uint8_t port, uint8_t val);
void     delay_ms(uint16_t ms);  
unsigned int val[]={0x3F, 0x06, 0x5B, 0x4F, 0x66, 0x6D, 0x7D, 0x07, 0x7F, 0x6F};
int main()
{
    void     gpio_port_write(0, val[]);
    for()

    return 0;
}
